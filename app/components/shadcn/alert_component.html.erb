<div data-slot="alert" role="alert" class="<%= alert_classes %>" <%= tag_attributes %>>
  <%= icon if icon? %>
  <%= title if title? %>
  <%= description if description? %>
  <% if content.present? %>
    <div class="col-start-2"><%= content %></div>
  <% end %>
</div>
